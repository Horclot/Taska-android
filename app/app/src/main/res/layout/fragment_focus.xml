<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0F1115">

    <HorizontalScrollView
        android:id="@+id/dateScrollView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#1C1F26"
        android:elevation="4dp"
        app:layout_behavior="com.google.android.material.appbar.AppBarLayout$ScrollingViewBehavior">

        <LinearLayout
            android:id="@+id/dateContainer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:paddingVertical="12dp">
        </LinearLayout>
    </HorizontalScrollView>

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="70dp"
        android:overScrollMode="never"
        android:padding="16dp">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="#1C1F26"
            android:clipToPadding="false"
            android:elevation="6dp"
            android:padding="16dp">

            <!-- Фоновые линии для плавных переходов -->
            <View
                android:id="@+id/backgroundTimeline"
                android:layout_width="2dp"
                android:layout_height="match_parent"
                android:layout_marginStart="48dp"
                android:background="@drawable/gradient_line_gray"
                android:alpha="0.3"/>

            <!-- Текущая временная линия (динамическая) с градиентом -->
            <View
                android:id="@+id/currentTimeLine"
                android:layout_width="3dp"
                android:layout_height="0dp"
                android:background="@drawable/gradient_timeline"
                android:visibility="gone"
                android:elevation="10dp"
                android:layout_marginStart="48dp"
                android:alpha="0.8"/>

            <!-- Контейнер для часовых делений -->
            <LinearLayout
                android:id="@+id/hourMarkersContainer"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:layout_marginStart="48dp"
                android:layout_marginTop="16dp"/>

            <RelativeLayout
                android:id="@+id/timelineWrapper"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp">

                <LinearLayout
                    android:id="@+id/timelineContainer"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical" />

            </RelativeLayout>

        </RelativeLayout>
    </androidx.core.widget.NestedScrollView>

    <!-- Кнопка добавления -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabAddTask"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="24dp"
        android:src="@drawable/ic_add_neon"
        app:backgroundTint="#FF6B6B"
        app:elevation="8dp"
        app:tint="@android:color/white"
        tools:ignore="SpeakableTextPresentCheck" />

    <!-- Прогресс бар -->
    <ProgressBar
        android:id="@+id/loadingProgress"
        style="?android:attr/progressBarStyleLarge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:visibility="gone"
        android:indeterminateTint="#FF6B6B"/>

</androidx.coordinatorlayout.widget.CoordinatorLayout>